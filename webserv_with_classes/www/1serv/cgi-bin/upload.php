#!/usr/bin/env php

<?php
// Set the Content-Type for the response

print_r($_FILES);

// print(php_sapi_name());
// $input = file_get_contents('php://stdin');
// print($input);
// Make sure the PHP script is being run from the command line
// if (php_sapi_name() == 'cli') {
//     // Read all input from standard input (stdin)
//     $input = file_get_contents('php://stdin');
//     // Process the input
//     echo "Received input: " . $input . "\n";
// } else {
//     echo "This script is intended to be run as a CGI script with stdin.\n";
// }
?>